# DISABLE auto-exit features that aren't working
auto_exit_config.MODE = "off"  # WAS "hard"
sonar_exit_config.MODE = "off"

# DISABLE trailing SL (not working)
trailing_config.ENABLED = False

# DISABLE exposure gate (not working)
exposure_config.MODE = "off"