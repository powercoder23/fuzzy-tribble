# Shell scripts must always use LF line endings. CRLF (\r) breaks the
# shebang (#!/bin/bash) when the file is checked out on Linux / the NAS,
# producing: "/bin/bash^M: bad interpreter: No such file or directory".
*.sh text eol=lf
