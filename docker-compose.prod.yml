services:
  scanner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: discount-scanner
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    command: python main.py
