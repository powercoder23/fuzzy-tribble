# -*- coding: utf-8 -*-
"""market_context.features — pure estimators + the feature-vector builder.

`estimators` is deliberately free of I/O so every formula is unit-testable
against hand-computed values. `builder` does the reading and persisting.
"""
